/**
 * Created by Victoria on 14.01.2017.
 */
;(function() {

  window.onload = function() {
    /*
    var products = document.querySelector('ul');

    products.addEventListener('mouseover', focusOnElement);

      function focusOnElement() {

         var button = products.createElement('button');

          alert('it is working');
          products.appendChild(button);
          button.innerHTML = 'Add to Card';


          for (var i = 0; i < a.length; i++) {
            if (a[i].innerHTML == "Add to Card") {
              button = a[i];
            }
          }
            button.classList.toggle('hide');
        }

      }
     */
  };
}());
